name: Build MOZ

on: [push, pull_request]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: cp ./docker/build/mozilla/build_local.sh . && ./build_local.sh .docker/build/mozilla/config.json
